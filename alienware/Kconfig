if VENDOR_ALIENWARE

choice
	prompt "Mainboard model"

source "src/mainboard/alienware/*/Kconfig.name"

endchoice

source "src/mainboard/alienware/*/Kconfig"

config MAINBOARD_VENDOR
	string
	default "ALIENWARE"

endif # VENDOR_ALIENWARE
